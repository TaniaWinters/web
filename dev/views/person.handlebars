
  <div class="card mb-3" style="max-width: 540px;">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{person.imageurl}}" class="img-fluid rounded-start" alt="client image">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">Client</h5>
        <p class="card-text">{{person.name}}</p>
        <p class="card-text"><small class="text-muted">Last updated 3 mins ago</small></p>
        {{#if person.dob}} 

<h6>DOB: {{person.dob}}</h6> 

{{/if}} 
    <h4 class="btn btn-primary">{{#each person.hobbies}}</h4>
    <li style="list-style: none;">{{this}}</li>
  {{/each}}</a>
  </div>
</div>
</div>
      </div>
    </div>
  </div>
</div>

  
{{#each person.visits}}

<div class="container">
  <div class="row">
    <div class="col">
      {{this.date}}
    </div>
    <div class="col">
      {{this.task}}
    </div>
    <div class="col">
        {{this.product}}
    </div>
  </div>
</div>
{{/each}}
